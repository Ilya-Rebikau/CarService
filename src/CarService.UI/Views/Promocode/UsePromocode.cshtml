@model PromocodeViewModel
@{
    ViewData["Title"] = "Использование промокода";
}
<div class="row d-flex justify-content-center pt-5 mt-5">
    <form asp-action="UsePromocode" asp-controller="Promocode" class="col-md-5 border rounded
            border-dark py-5 px-5 bg-light bg-gradient">
        <p class="fs-3 text-center row pb-3  d-flex justify-content-center">Использование промокода</p>
        <div asp-validation-summary="All" class="text-danger"></div>
        <div class="form-group row pb-3">
            <label asp-for="Text" class="col-sm-4 col-form-label"></label>
            <div class="col-sm-8">
                <input type="text" asp-for="Text" required class="form-control shadow-none" />
            </div>
        </div>
        <div class="d-flex justify-content-center">
            <div class="form-group">
                <input type="submit" value="Использовать" class="btn btn-dark shadow-none" />
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}